version: '2'

services:
  webapi-demo:
    image: webapi-demo:debug
    build:
      context: .
      dockerfile: Dockerfile.debug
    ports:
      - "80:80"
    environment:
      - REMOTE_DEBUGGING
